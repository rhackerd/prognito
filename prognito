
test="$(pwd)"


cd ~/Dokumenty/prognito || { echo "Directory not found"; exit 1; }

if command -v python3 &>/dev/null; then
    echo "Running python script"
else
    echo "Please install python3"
    exit
fi

# Run the Python script with the directory argument
python main.py "$@" -d "$test"